# 🏠 PasitDev - ระบบประเมินราคาทรัพย์สินอัจฉริยะ

> ระบบประเมินราคาอสังหาริมทรัพย์ด้วย AI แบบ Local ที่ใช้เทคโนโลยี **Ollama AI**, **Flask**, **jQuery** และ **HTML5** พร้อมรองรับ **77 จังหวัดทั่วประเทศไทย** และสร้าง **PDF รายงานภาษาไทยได้อย่างสมบูรณ์**

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![Flask](https://img.shields.io/badge/Flask-3.0.0-green.svg)](https://flask.palletsprojects.com/)
[![Ollama](https://img.shields.io/badge/Ollama-AI-orange.svg)](https://ollama.ai/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Status](https://img.shields.io/badge/Status-Production%20Ready-brightgreen.svg)]()

## 📑 สารบัญ

- [🌟 ไฮไลท์โปรเจค](#ไฮไลท์โปรเจค)
- [✨ ฟีเจอร์หลัก](#ฟีเจอร์หลัก)
- [🛠️ เทคโนโลยีที่ใช้](#เทคโนโลジที่ใช้)
- [🤔 ทำไมต้อง Ollama?](#ทำไมต้อง-ollama)
- [📊 เปรียบเทียบ AI Models](#เปรียบเทียบ-ai-models-สำหรับ-ollama)
- [📸 ภาพหน้าจอ](#ภาพหน้าจอ)
- [🚀 การติดตั้ง](#การติดตั้ง)
  - [ขั้นตอนที่ 1: Clone Repository](#ขั้นตอนที่-1-clone-repository)
  - [ขั้นตอนที่ 2: ติดตั้ง Python Dependencies](#ขั้นตอนที่-2-ติดตั้ง-python-dependencies)
  - [ขั้นตอนที่ 3: ติดตั้ง Ollama](#ขั้นตอนที่-3-ติดตั้ง-ollama-สำคัญมาก)
  - [ขั้นตอนที่ 4: ดาวน์โหลด AI Model](#ขั้นตอนที่-4-ดาวน์โหลด-ai-model)
  - [ขั้นตอนที่ 5: เปิดใช้งาน Ollama Server](#ขั้นตอนที่-5-เปิดใช้งาน-ollama-server)
  - [ขั้นตอนที่ 6: เริ่มต้น Web Application](#ขั้นตอนที่-6-เริ่มต้น-web-application)
- [🎯 Quick Start Guide](#quick-start-guide-เริ่มใช้งานด่วน)
- [📖 การใช้งาน](#การใช้งาน)
- [🔌 API Endpoints](#api-endpoints)
- [📁 โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
- [⚙️ การปรับแต่ง](#การปรับแต่ง)
- [🐛 การแก้ปัญหา](#การแก้ปัญหา)
- [🚀 การพัฒนาต่อ](#การพัฒนาต่อ)
- [📞 การติดต่อและสนับสนุน](#การติดต่อและสนับสนุน)
- [🎓 เรียนรู้เพิ่มเติม](#เรียนรู้เพิ่มเติม)

---

## 🌟 ไฮไลท์โปรเจค

### 🎯 ทำอะไรได้บ้าง?

**PasitDev** Asset เป็นระบบประเมินราคาอสังหาริมทรัพย์ที่ใช้ **AI แบบ Local** (Ollama) ช่วยวิเคราะห์และประเมินราคาทรัพย์สินอย่างแม่นยำ โดยไม่ต้องส่งข้อมูลออกนอกเครื่อง

### ⚡ ใช้งานได้ 2 โหมด:

#### 1. 🤖 **AI Mode** - ประเมินด้วย AI ที่ฉลาดและละเอียด
- วิเคราะห์ปัจจัยหลากหลาย: ทำเล, สภาพ, อายุ, ความต้องการในตลาด
- ให้คำแนะนำและคำอธิบายอย่างละเอียด
- รองรับภาษาไทย 100%
- ใช้เวลาประมาณ 30-60 วินาที

#### 2. ⚡ **Quick Estimate** - ประมาณราคาแบบเร็ว
- คำนวณทันที ไม่ต้องรอ AI
- ใช้ฐานข้อมูลราคาและตัวคูณจากทั้ง 77 จังหวัด
- เหมาะสำหรับเช็คราคาคร่าวๆ อย่างรวดเร็ว

### 🔥 จุดเด่น:

| คุณสมบัติ | รายละเอียด |
|-----------|-----------|
| 🆓 **ฟรี 100%** | ไม่ต้องจ่ายค่า API, ไม่ต้อง Credit Card |
| 🔒 **Privacy First** | ข้อมูลไม่ออกจากเครื่อง, ปลอดภัย 100% |
| 🇹🇭 **รองรับภาษาไทยเต็มรูปแบบ** | ทั้ง UI, AI, และ PDF Export |
| 📍 **77 จังหวัด** | ครอบคลุมทั่วประเทศไทย |
| 📄 **PDF Export** | สร้างรายงานสวยงาม รองรับภาษาไทยชัดเจน |
| 📊 **Excel Upload** | นำเข้าข้อมูลราคาจาก Excel/CSV |
| 🎨 **UI สวยงาม** | Responsive, ใช้งานได้ทุกอุปกรณ์ |
| ⚡ **เร็ว** | ทำงานแบบ Local, ไม่พึ่งพา Internet |

### 📋 ประเภททรัพย์สินที่รองรับ:
- 🏢 **คอนโด** (Condominium)
- 🏠 **บ้านเดี่ยว** (Single House)
- 🏘️ **ทาวน์เฮาส์** (Townhouse)
- 🏬 **อาคารพาณิชย์** (Commercial Building)
- 🌾 **ที่ดิน** (Land)

### 🚀 เริ่มต้นใช้งานภายใน 10 นาที!
1. ติดตั้ง Python Dependencies (2 นาที)
2. ติดตั้ง Ollama (3 นาที)
3. ดาวน์โหลด AI Model (5 นาที)
4. เริ่มใช้งาน! 🎉

> **หมายเหตุ**: ต้องการ RAM อย่างน้อย 8 GB สำหรับ AI Models

---

## ✨ ฟีเจอร์หลัก

### 🤖 AI-Powered Valuation
- **ประเมินราคาด้วย AI**: ใช้ Ollama AI (llama3.2) วิเคราะห์และประเมินราคาทรัพย์สินอย่างละเอียด
- **การวิเคราะห์แบบองค์รวม**: AI พิจารณาปัจจัยหลากหลาย เช่น ทำเล, สภาพ, อายุ, ความต้องการในตลาด

### ⚡ Quick Estimate
- **ประมาณราคาเร็ว**: คำนวณราคาเบื้องต้นแบบทันที ไม่ต้องรอ AI
- **รองรับ 77 จังหวัด**: เลือกจากจังหวัดทั่วประเทศไทยพร้อมตัวคูณราคาที่แตกต่าง

### 📄 PDF Export (รองรับภาษาไทย 100%)
- **ดาวน์โหลด PDF**: สร้างรายงานประเมินราคาเป็น PDF สวยงาม
- **ภาษาไทยชัดเจน**: ใช้ xhtml2pdf + reportlab แสดงผลภาษาไทยได้สมบูรณ์
- **การออกแบบทันสมัย**: HTML + CSS สวยงาม พร้อมตาราง สี และรูปแบบมืออาชีพ

### 📊 Excel Upload
- **อัปโหลดข้อมูลราคา**: นำเข้าข้อมูลราคาจาก Excel/CSV
- **Template Excel**: ดาวน์โหลด template พร้อมใช้งาน
- **รองรับไฟล์หลากหลาย**: .xlsx, .xls, .csv (ขนาดไม่เกิน 16MB)

### 🎨 UI/UX ทันสมัย
- **Responsive Design**: ใช้งานได้บนทุกอุปกรณ์ (Desktop, Tablet, Mobile)
- **Gradient & Animations**: ออกแบบด้วย CSS3 Animations ที่สวยงาม
- **Real-time Status**: ตรวจสอบสถานะ Ollama แบบเรียลไทม์

### 🗺️ รองรับทุกจังหวัด
- **77 จังหวัด**: ครอบคลุมทั่วประเทศไทย
- **ตัวคูณราคาตามภูมิภาค**: แต่ละจังหวัดมีตัวคูณตามสภาพเศรษฐกิจและความนิยม
- **จัดกลุ่มตามภูมิภาค**: กลาง, เหนือ, อีสาน, ใต้

## 🛠️ เทคโนโลยีที่ใช้

### Backend
- **Python 3.11+**
- **Flask 3.0.0** - Web Framework
- **Ollama** - AI Model (llama3.2)
- **xhtml2pdf 0.2.16** - PDF Generation (รองรับภาษาไทย)
- **reportlab 4.0.7** - PDF Library
- **Pandas 2.1.4** - Data Processing
- **OpenPyXL 3.1.2** - Excel File Handling

### Frontend
- **HTML5** - Structure
- **CSS3** - Styling & Animations
- **jQuery 3.6.0** - DOM Manipulation & AJAX
- **Font Awesome 6.4.0** - Icons
- **Google Fonts (Sarabun)** - Thai Typography

### AI & ML
- **Ollama** - Local AI Model Server
- **llama3.2** - Language Model (ตั้งค่าได้)

---

## 🤔 ทำไมต้อง Ollama?

### ข้อดีของการใช้ Ollama (Local AI)

#### 🔒 ความเป็นส่วนตัว
- **ข้อมูลไม่ออกจากเครื่อง**: ทุกอย่างทำงานบนเครื่องคุณ ไม่ส่งข้อมูลไปยัง Cloud
- **ไม่ต้องกังวลเรื่อง Privacy**: ข้อมูลทรัพย์สินและราคาเป็นความลับ
- **ปลอดภัย 100%**: ไม่มีการเก็บ log หรือส่งข้อมูลออกนอกระบบ

#### 💰 ฟรี ไม่มีค่าใช้จ่าย
- **ไม่ต้องจ่าย API Key**: ไม่เหมือน OpenAI, Claude, หรือ Gemini
- **ใช้ไม่อ้ันไม่จำกัด**: ประเมินราคากี่ครั้งก็ได้ ไม่มีค่าใช้จ่ายเพิ่ม
- **ไม่ต้องสมัครบัญชี**: ไม่ต้องมี Credit Card หรือสมัครใช้บริการใดๆ

#### ⚡ เร็วและเสถียร
- **ไม่ต้องรอ Internet**: ทำงานได้แม้ไม่มีเน็ต (หลังติดตั้ง Model แล้ว)
- **ไม่มี Rate Limit**: ไม่โดนจำกัดจำนวนคำขอต่อนาที
- **เสถียร**: ไม่พึ่งพา Cloud Service ที่อาจมีปัญหา downtime

#### 🎛️ ควบคุมได้เต็มที่
- **เลือก Model เอง**: ใช้ llama3.2, mistral, gemma2 หรือ model ใดก็ได้
- **ปรับแต่งได้**: ปรับ temperature, context window, system prompt ตามต้องการ
- **อัปเกรดเมื่อไหร่ก็ได้**: Model ใหม่ๆ มีให้ดาวน์โหลดฟรีตลอด

### เปรียบเทียบ Ollama vs Cloud AI

| คุณสมบัติ | Ollama (Local) | OpenAI API | Google Gemini | Claude API |
|-----------|---------------|------------|---------------|------------|
| **ค่าใช้จ่าย** | ฟรี ✅ | $0.002-0.03/1K tokens | $0.0001-0.015/1K tokens | $0.003-0.015/1K tokens |
| **Privacy** | 100% ไม่ส่งข้อมูลออก ✅ | ส่งไปยัง OpenAI | ส่งไปยัง Google | ส่งไปยัง Anthropic |
| **Internet** | ไม่จำเป็น (หลังติดตั้ง) ✅ | จำเป็น | จำเป็น | จำเป็น |
| **Rate Limit** | ไม่มี ✅ | 3-200 RPM | 15-60 RPM | 5-50 RPM |
| **Latency** | เร็ว (local) ✅ | ปานกลาง | ปานกลาง | ปานกลาง |
| **API Key** | ไม่ต้องการ ✅ | ต้องมี | ต้องมี | ต้องมี |
| **Credit Card** | ไม่ต้องการ ✅ | ต้องมี | ต้องมี (บางระดับ) | ต้องมี |
| **Customization** | ปรับแต่งได้เต็มที่ ✅ | จำกัด | จำกัด | จำกัด |
| **ความแม่นยำ** | ดีมาก (ขึ้นกับ model) | ดีเยี่ยม | ดีเยี่ยม | ดีเยี่ยม |

---

## 📊 เปรียบเทียบ AI Models สำหรับ Ollama

เลือก Model ที่เหมาะกับเครื่องคุณ:

| Model | ขนาด | RAM ต้องการ | ความเร็ว | ภาษาไทย | แนะนำสำหรับ |
|-------|------|-------------|----------|---------|-------------|
| **llama3.2:1b** ⚡ | 1.3 GB | 4 GB | ⚡⚡⚡⚡⚡ เร็วมาก | ⭐⭐⭐ ดี | เครื่องสเปคต่ำ, ต้องการความเร็ว |
| **llama3.2:3b** 🌟 | 2.0 GB | 8 GB | ⚡⚡⚡⚡ เร็ว | ⭐⭐⭐⭐ ดีมาก | **แนะนำ** - สมดุลระหว่างเร็วและฉลาด |
| **llama3.1:8b** | 4.7 GB | 16 GB | ⚡⚡⚡ ปานกลาง | ⭐⭐⭐⭐⭐ ดีเยี่ยม | เครื่องสเปคสูง, ต้องการความแม่นยำ |
| **mistral:7b** | 4.1 GB | 12 GB | ⚡⚡⚡⚡ เร็ว | ⭐⭐⭐ ดี | ทางเลือกจาก Llama |
| **gemma2:9b** | 5.4 GB | 16 GB | ⚡⚡⚡ ปานกลาง | ⭐⭐⭐⭐ ดีมาก | จาก Google, รองรับหลายภาษา |
| **qwen2.5:7b** | 4.7 GB | 12 GB | ⚡⚡⚡ ปานกลาง | ⭐⭐⭐⭐⭐ ดีเยี่ยม | ออกแบบสำหรับภาษาเอเชีย |

### 🎯 คำแนะนำการเลือก Model:

**เครื่อง RAM 8GB หรือน้อยกว่า:**
```bash
ollama pull llama3.2:1b  # เล็กสุด เร็วสุด
ollama pull llama3.2     # สมดุลดี
```

**เครื่อง RAM 16GB:**
```bash
ollama pull llama3.2     # แนะนำ
ollama pull llama3.1     # ฉลาดกว่า แต่ช้ากว่า
ollama pull mistral      # ทางเลือก
```

**เครื่อง RAM 32GB ขึ้นไป:**
```bash
ollama pull llama3.1     # ดีที่สุด
ollama pull qwen2.5      # เยี่ยมสำหรับภาษาไทย
ollama pull gemma2:9b    # จาก Google
```

**มี GPU (NVIDIA/AMD/Apple Silicon):**
```bash
# Model ใหญ่ทำงานได้เร็วมาก
ollama pull llama3.1:70b  # สุดยอด (ต้องการ VRAM 40GB+)
ollama pull llama3.1      # แนะนำ
```

### ทดสอบ Model:
```bash
# ทดสอบความเร็วและคุณภาพ
ollama run llama3.2
>>> ประเมินราคาคอนโดขนาด 50 ตร.ม. ใกล้ BTS ในกรุงเทพ

# ถ้าไม่พอใจ ลองเปลี่ยน model
ollama pull llama3.1
ollama run llama3.1
>>> ประเมินราคาคอนโดขนาด 50 ตร.ม. ใกล้ BTS ในกรุงเทพ
```

---

## 📸 ภาพหน้าจอ

```
[Hero Section]
- แสดง stat cards: ทรัพย์สินที่ประเมิน, ผู้ใช้งาน, ความแม่นยำ
- ปุ่มเริ่มประเมิน, ประมาณราคาเร็ว, จัดการข้อมูล

[Evaluation Form]
- เลือกประเภททรัพย์สิน, จังหวัด (77 จังหวัด)
- กรอกขนาด, ห้องนอน, ห้องน้ำ, อายุ, สภาพ
- ปุ่มประเมินด้วย AI

[Result Card]
- แสดงข้อมูลทรัพย์สินสรุป
- ผลการประเมินจาก AI
- ดาวน์โหลด PDF, แชร์
```

## 🚀 การติดตั้ง

### ✅ ข้อกำหนดระบบ

#### ฮาร์ดแวร์ขั้นต่ำ
- **RAM**: 8 GB ขึ้นไป (แนะนำ 16 GB สำหรับ AI models)
- **Storage**: 10 GB ว่าง (สำหรับ Ollama และ Models)
- **CPU**: Dual-core ขึ้นไป (แนะนำ Quad-core)
- **GPU**: ไม่จำเป็น แต่จะช่วยให้ AI ทำงานเร็วขึ้น (NVIDIA CUDA, AMD ROCm, Apple Silicon)

#### ซอฟต์แวร์
- **Python**: 3.11 ขึ้นไป (Python 3.11, 3.12 หรือ 3.13)
- **pip**: ตัวจัดการแพ็กเกจของ Python
- **Ollama**: Local AI runtime (จะติดตั้งในขั้นตอนถัดไป)
- **Operating System**:
  - macOS 11 (Big Sur) ขึ้นไป
  - Linux (Ubuntu 18.04+, Debian 10+, Fedora 28+)
  - Windows 10/11 (WSL2 หรือ Native)

---

## 📥 ขั้นตอนการติดตั้งแบบละเอียด

### ขั้นตอนที่ 1: Clone Repository

```bash
# Clone โปรเจค
git clone https://github.com/ezynook/Python-Asset.git
cd Python-Asset

# หรือถ้าโหลด ZIP มาแตกไฟล์
cd PasitDev
```

---

### ขั้นตอนที่ 2: ติดตั้ง Python Dependencies

#### 2.1 ตรวจสอบเวอร์ชัน Python
```bash
python3 --version
# ควรแสดง Python 3.11.x หรือสูงกว่า
```

#### 2.2 สร้าง Virtual Environment (แนะนำ)
```bash
# สร้าง virtual environment
python3 -m venv venv

# เปิดใช้งาน virtual environment
# สำหรับ macOS/Linux:
source venv/bin/activate

# สำหรับ Windows (PowerShell):
venv\Scripts\Activate.ps1

# สำหรับ Windows (CMD):
venv\Scripts\activate.bat
```

#### 2.3 อัปเกรด pip และติดตั้ง Dependencies
```bash
# อัปเกรด pip เป็นเวอร์ชันล่าสุด
pip install --upgrade pip

# ติดตั้ง dependencies ทั้งหมด
pip install -r requirements.txt --no-cache-dir

# ตรวจสอบว่าติดตั้งสำเร็จ
pip list
```

**📦 Packages ที่จะถูกติดตั้ง:**
- `Flask==3.0.0` - Web framework
- `xhtml2pdf==0.2.16` - PDF generation (รองรับไทย)
- `reportlab==4.0.7` - PDF library
- `pandas==2.1.4` - Data processing
- `openpyxl==3.1.2` - Excel file handling
- `requests` - HTTP library

**หมายเหตุ**: ไม่ต้องติดตั้ง system dependencies เพิ่มเติม ทุกอย่างอยู่ใน Python packages แล้ว!

---

### ขั้นตอนที่ 3: ติดตั้ง Ollama (สำคัญมาก!)

Ollama คือ local AI runtime ที่ช่วยให้คุณรัน Large Language Models (LLMs) บนเครื่องตัวเอง โดยไม่ต้องส่งข้อมูลไปยัง cloud

#### 🍎 สำหรับ macOS

##### วิธีที่ 1: ดาวน์โหลด Installer (แนะนำสำหรับมือใหม่)
```bash
# 1. เปิด Browser ไปที่ https://ollama.ai/download
# 2. คลิก "Download for macOS"
# 3. เปิดไฟล์ Ollama.dmg ที่ดาวน์โหลดมา
# 4. ลากไอคอน Ollama ไปยัง Applications folder
# 5. เปิด Ollama จาก Applications
```

##### วิธีที่ 2: ติดตั้งผ่าน Terminal (สำหรับ Advanced Users)
```bash
# ดาวน์โหลดและติดตั้งด้วยคำสั่งเดียว
curl -fsSL https://ollama.ai/install.sh | sh

# รอสักครู่จนติดตั้งเสร็จ
# Ollama จะถูกติดตั้งที่ /usr/local/bin/ollama
```

##### วิธีที่ 3: ติดตั้งผ่าน Homebrew
```bash
# ถ้ามี Homebrew ติดตั้งอยู่แล้ว
brew install ollama

# เริ่มต้น Ollama service
brew services start ollama
```

##### ตรวจสอบการติดตั้ง (macOS)
```bash
# ตรวจสอบว่าติดตั้งสำเร็จ
ollama --version
# ควรแสดง: ollama version 0.x.x

# ตรวจสอบว่า Ollama กำลังทำงาน
curl http://localhost:11434/api/tags
# ถ้าได้ JSON response แสดงว่าทำงานแล้ว
```

---

#### 🐧 สำหรับ Linux (Ubuntu/Debian/Fedora)

##### วิธีที่ 1: ติดตั้งด้วย Shell Script (แนะนำ)
```bash
# ดาวน์โหลดและติดตั้งอัตโนมัติ
curl -fsSL https://ollama.ai/install.sh | sh

# หรือถ้าต้องการดู script ก่อน:
curl -fsSL https://ollama.ai/install.sh -o install.sh
cat install.sh  # ตรวจสอบ
sh install.sh
```

##### วิธีที่ 2: ติดตั้งด้วย Package Manager

**Ubuntu/Debian:**
```bash
# เพิ่ม Ollama repository
curl -fsSL https://ollama.ai/install.sh | sh

# หรือติดตั้งด้วย APT (ถ้ามี repo)
sudo apt update
sudo apt install ollama
```

**Fedora/RHEL/CentOS:**
```bash
# ติดตั้งด้วย script
curl -fsSL https://ollama.ai/install.sh | sh
```

##### เริ่มต้น Ollama Service บน Linux
```bash
# เริ่มต้น Ollama service
sudo systemctl start ollama

# ตั้งค่าให้เริ่มอัตโนมัติเมื่อ boot
sudo systemctl enable ollama

# ตรวจสอบสถานะ
sudo systemctl status ollama
```

##### ตรวจสอบการติดตั้ง (Linux)
```bash
# ตรวจสอบเวอร์ชัน
ollama --version

# ทดสอบการเชื่อมต่อ
curl http://localhost:11434/api/tags
```

---

#### 🪟 สำหรับ Windows

##### วิธีที่ 1: ติดตั้ง Native Windows App (แนะนำ)
```powershell
# 1. ดาวน์โหลด Windows Installer
# เปิด Browser ไปที่ https://ollama.ai/download/windows

# 2. ดาวน์โหลดไฟล์ OllamaSetup.exe

# 3. ดับเบิลคลิกไฟล์เพื่อติดตั้ง

# 4. ปฏิบัติตามขั้นตอนการติดตั้ง

# 5. เปิด Command Prompt หรือ PowerShell และทดสอบ:
ollama --version
```

##### วิธีที่ 2: ติดตั้งผ่าน WSL2 (Windows Subsystem for Linux)
```bash
# 1. เปิด WSL2 Terminal (Ubuntu)
# 2. ติดตั้ง Ollama ใน WSL
curl -fsSL https://ollama.ai/install.sh | sh

# 3. เริ่มต้น Ollama
ollama serve
```

##### ตรวจสอบการติดตั้ง (Windows)
```powershell
# เปิด PowerShell หรือ CMD
ollama --version

# ทดสอบการเชื่อมต่อ
curl http://localhost:11434/api/tags
```

---

### ขั้นตอนที่ 4: ดาวน์โหลด AI Model

หลังจากติดตั้ง Ollama เรียบร้อยแล้ว คุณต้องดาวน์โหลด AI Model มาใช้งาน

#### 4.1 เริ่มต้น Ollama Server (ถ้ายังไม่ได้เปิด)
```bash
# เปิด Terminal หรือ CMD แยกต่างหาก และรันคำสั่งนี้
ollama serve

# หน้าต่างนี้จะแสดงข้อความว่า Ollama กำลังทำงาน
# ใช้งานต่อใน Terminal อีกหน้าต่างหนึ่ง
```

#### 4.2 ดาวน์โหลด Models (เปิด Terminal ใหม่)

**🌟 แนะนำ: llama3.2 (รองรับภาษาไทยดี, ขนาดพอดี)**
```bash
# ดาวน์โหลด llama3.2 (3B parameters - ขนาด ~2GB)
ollama pull llama3.2

# รอจนดาวน์โหลดเสร็จ (5-10 นาที ขึ้นอยู่กับความเร็วเน็ต)
```

**Models อื่นๆ ที่แนะนำ:**
```bash
# llama3.2:1b - เล็กสุด, เร็วสุด (ขนาด ~1.3GB)
ollama pull llama3.2:1b

# llama3.1 - ใหญ่กว่า, ฉลาดกว่า (ขนาด ~4.7GB)
ollama pull llama3.1

# mistral - ทางเลือกจาก Mistral AI (ขนาด ~4.1GB)
ollama pull mistral

# gemma2 - จาก Google (ขนาด ~5.4GB)
ollama pull gemma2:9b

# qwen2.5 - รองรับหลายภาษารวมไทย (ขนาด ~4.7GB)
ollama pull qwen2.5
```

#### 4.3 ตรวจสอบ Models ที่ดาวน์โหลดแล้ว
```bash
# แสดงรายการ models ทั้งหมด
ollama list

# ผลลัพธ์จะเป็นแบบนี้:
# NAME              ID              SIZE      MODIFIED
# llama3.2:latest   a80c4f17acd5    2.0 GB    2 minutes ago
# mistral:latest    f974a74358d6    4.1 GB    1 hour ago
```

#### 4.4 ทดสอบ Model
```bash
# ทดสอบคุยกับ AI
ollama run llama3.2

# ลองถาม: "สวัสดีครับ คุณชื่ออะไร"
# กด Ctrl+D หรือพิมพ์ /bye เพื่อออก
```

---

### ขั้นตอนที่ 5: เปิดใช้งาน Ollama Server

Ollama ต้องทำงานในพื้นหลังตลอดเวลาที่คุณใช้งานระบบนี้

#### วิธีที่ 1: รันใน Foreground (เห็นข้อความ Log)
```bash
# เปิด Terminal แยกต่างหากและรันคำสั่งนี้
ollama serve

# เอาหน้าต่างนี้ค้างไว้ จะเห็น log แบบนี้:
# time=2025-01-24T10:00:00.000+07:00 level=INFO msg="Listening on 127.0.0.1:11434"
# time=2025-01-24T10:00:01.000+07:00 level=INFO msg="model loaded"
```

#### วิธีที่ 2: รันใน Background (macOS/Linux)
```bash
# รันแบบ background service
nohup ollama serve > /dev/null 2>&1 &

# หรือใช้ systemd (Linux)
sudo systemctl start ollama
sudo systemctl enable ollama  # เปิดอัตโนมัติเมื่อ boot
```

#### วิธีที่ 3: รันอัตโนมัติ (macOS)
```bash
# ถ้าติดตั้งด้วย Homebrew
brew services start ollama

# ตรวจสอบสถานะ
brew services list
```

#### ตรวจสอบว่า Ollama ทำงานอยู่
```bash
# ทดสอบการเชื่อมต่อ
curl http://localhost:11434/api/tags

# ถ้าได้ JSON response แสดงว่าทำงานปกติ
# ผลลัพธ์: {"models":[...]}
```

---

### ขั้นตอนที่ 6: เริ่มต้น Web Application

#### 6.1 เปิด Terminal ใหม่ (แยกจาก Ollama)
```bash
# ไปยัง project directory
cd /path/to/PasitDev

# เปิดใช้ virtual environment (ถ้าใช้)
source venv/bin/activate  # macOS/Linux
# หรือ
venv\Scripts\activate  # Windows
```

#### 6.2 รัน Flask Application
```bash
# เริ่มต้น Flask server
python app.py

# หรือถ้าใช้ python3
python3 app.py
```

#### 6.3 เปิดเว็บไซต์
```bash
# เว็บไซต์จะเปิดที่:
# 🌐 http://localhost:8088

# เปิด Browser และไปที่ URL ข้างบน
```

**ข้อความที่จะเห็นใน Terminal:**
```
 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment.
 * Running on http://127.0.0.1:8088
 * Running on http://192.168.x.x:8088
Press CTRL+C to quit
```

---

## 🎯 Quick Start Guide (เริ่มใช้งานด่วน)

หลังจากติดตั้งครบแล้ว ทำตามขั้นตอนนี้ทุกครั้งที่ต้องการใช้งาน:

```bash
# Terminal 1: เปิด Ollama Server
ollama serve

# Terminal 2: เปิด Web Application
cd /path/to/PasitDev
source venv/bin/activate  # ถ้าใช้ virtual env
python app.py

# Browser: เปิด http://localhost:8088
```

**แนะนำ**: ใช้ Terminal Multiplexer เช่น `tmux` หรือ `screen` เพื่อจัดการหลายหน้าต่าง

---

## 📖 การใช้งาน

### 1. ประเมินราคาด้วย AI

1. คลิกปุ่ม **"เริ่มประเมินราคา"** หรือเลื่อนไปที่ส่วนฟอร์ม
2. กรอกข้อมูลทรัพย์สิน:
   - **ประเภททรัพย์สิน**: คอนโด, บ้านเดี่ยว, ทาวน์เฮาส์, อาคารพาณิชย์, ที่ดิน
   - **จังหวัด**: เลือกจาก 77 จังหวัด (จัดกลุ่มตามภูมิภาค)
   - **ขนาดพื้นที่**: ตารางเมตร
   - **จำนวนห้องนอน**: (ถ้ามี)
   - **จำนวนห้องน้ำ**: (ถ้ามี)
   - **อายุอาคาร**: ปี
   - **สภาพทรัพย์สิน**: ใหม่มาก, ดีมาก, ดี, ปานกลาง, ต้องซ่อมแซม
   - **ข้อมูลเพิ่มเติม**: เช่น มีที่จอดรถ, ใกล้ BTS, วิวสวย
3. คลิก **"ประเมินด้วย AI"**
4. รอ AI วิเคราะห์ (30-60 วินาที)
5. ดูผลการประเมินพร้อมคำแนะนำ

### 2. ประมาณราคาเร็ว

1. คลิกปุ่ม **"ประมาณราคาเร็ว"**
2. กรอกข้อมูลพื้นฐาน:
   - ประเภททรัพย์สิน
   - ขนาดพื้นที่
   - จังหวัด
3. คลิก **"คำนวณราคา"**
4. ได้ผลทันที (ไม่ใช้ AI)

### 3. ดาวน์โหลด PDF

1. หลังจากได้ผลการประเมินจาก AI
2. คลิกปุ่ม **"ดาวน์โหลด PDF"**
3. ไฟล์ PDF จะถูกดาวน์โหลดอัตโนมัติ
4. PDF รองรับภาษาไทย 100% ด้วย xhtml2pdf + reportlab

### 4. จัดการข้อมูลราคา (Upload Excel)

1. คลิกปุ่ม **"จัดการข้อมูลราคา"** ที่หน้าแรก
2. คลิก **"ดาวน์โหลด Template"** เพื่อดู format ไฟล์
3. กรอกข้อมูลในไฟล์ Excel:
   ```
   | province          | property_type | base_price_per_sqm |
   |-------------------|---------------|--------------------|
   | กรุงเทพมหานคร     | คอนโด         | 80000             |
   | เชียงใหม่         | บ้านเดี่ยว    | 45000             |
   | ภูเก็ต            | คอนโด         | 70000             |
   ```
4. เลือกไฟล์และคลิก **"อัปโหลดไฟล์"**
5. ข้อมูลจะถูกนำมาใช้ในการคำนวณ "ประมาณราคาเร็ว"

## 🔌 API Endpoints

### GET `/`
หน้าแรกของเว็บไซต์

### GET `/api/provinces`
ดึงข้อมูลจังหวัดทั้งหมด (77 จังหวัด)

**Response:**
```json
{
  "success": true,
  "provinces": [
    {
      "name": "กรุงเทพมหานคร",
      "region": "กลาง",
      "multiplier": 1.8
    },
    ...
  ],
  "total": 77
}
```

### POST `/api/evaluate`
ประเมินราคาด้วย AI

**Request Body:**
```json
{
  "property_type": "คอนโด",
  "location": "กรุงเทพมหานคร",
  "area": "50",
  "bedrooms": "2",
  "bathrooms": "2",
  "age": "5",
  "condition": "ดี",
  "additional_info": "ใกล้ BTS"
}
```

**Response:**
```json
{
  "success": true,
  "evaluation": "ผลการวิเคราะห์จาก AI...",
  "property_data": { ... }
}
```

### POST `/api/quick-estimate`
ประมาณราคาแบบเร็ว (ไม่ใช้ AI)

**Request Body:**
```json
{
  "property_type": "คอนโด",
  "area": "50",
  "province": "กรุงเทพมหานคร"
}
```

**Response:**
```json
{
  "success": true,
  "estimated_price": 4500000,
  "price_per_sqm": 90000,
  "area": 50,
  "province": "กรุงเทพมหานคร",
  "region": "กลาง",
  "multiplier": 1.8
}
```

### POST `/api/download-pdf`
สร้างและดาวน์โหลด PDF รายงาน

**Request Body:**
```json
{
  "property_data": { ... },
  "evaluation": "ผลการประเมิน..."
}
```

**Response:** PDF File (application/pdf)

### POST `/api/upload-price-data`
อัปโหลดข้อมูลราคาจาก Excel/CSV

**Form Data:** `file` (multipart/form-data)

**Response:**
```json
{
  "success": true,
  "message": "อัปโหลดสำเร็จ 10 รายการ",
  "updated_count": 10,
  "total_records": 10
}
```

### GET `/api/download-template`
ดาวน์โหลด Excel template

**Response:** Excel File (.xlsx)

### GET `/api/get-price-data`
ดึงข้อมูลราคาที่อัปโหลดไว้

**Response:**
```json
{
  "success": true,
  "data": [
    {
      "province": "กรุงเทพมหานคร",
      "property_type": "คอนโด",
      "base_price_per_sqm": 80000
    },
    ...
  ],
  "total": 10
}
```

### GET `/api/check-ollama`
ตรวจสอบสถานะ Ollama

**Response:**
```json
{
  "success": true,
  "connected": true,
  "models": ["llama3.2", "llama2"]
}
```

## 📁 โครงสร้างโปรเจค

```
PasitDev/
├── app.py                      # Flask Backend หลัก
├── requirements.txt            # Python Dependencies
├── README.md                   # เอกสารนี้
│
├── templates/
│   └── index.html             # หน้าเว็บหลัก
│
├── static/
│   ├── css/
│   │   └── style.css          # Stylesheet หลัก (Responsive)
│   │
│   ├── js/
│   │   └── main.js            # jQuery Scripts (AJAX, UI)
│   │
│   └── images/                # รูปภาพ (ถ้ามี)
│
└── uploads/                   # โฟลเดอร์สำหรับไฟล์ที่อัปโหลด
```

## ⚙️ การปรับแต่ง

### เปลี่ยน AI Model

แก้ไขไฟล์ `app.py` บรรทัดที่ 19:

```python
OLLAMA_MODEL = "llama3.2"  # เปลี่ยนเป็น mistral, codellama, etc.
```

### ปรับราคาพื้นฐานตามประเภททรัพย์สิน

แก้ไขไฟล์ `app.py` ฟังก์ชัน `quick_estimate()` (บรรทัดประมาณ 330):

```python
base_prices = {
    'คอนโด': 50000,        # บาท/ตร.ม.
    'บ้านเดี่ยว': 35000,
    'ทาวน์เฮาส์': 30000,
    'อาคารพาณิชย์': 40000,
    'ที่ดิน': 15000
}
```

### ปรับตัวคูณราคาจังหวัด

แก้ไขไฟล์ `app.py` ส่วน `PROVINCES_DATA` (บรรทัด 22-108):

```python
PROVINCES_DATA = {
    'กรุงเทพมหานคร': {'region': 'กลาง', 'multiplier': 1.8},
    'เชียงใหม่': {'region': 'เหนือ', 'multiplier': 1.4},
    # ... เปลี่ยนค่า multiplier ตามต้องการ
}
```

### เปลี่ยนสี Theme

แก้ไขไฟล์ `static/css/style.css` บรรทัด 10:

```css
:root {
    --primary-color: #2563eb;     /* สีหลัก */
    --secondary-color: #3b82f6;   /* สีรอง */
    --success-color: #10b981;
    --warning-color: #f59e0b;
    --danger-color: #ef4444;
}
```

### เปลี่ยน Port

แก้ไขไฟล์ `app.py` บรรทัดสุดท้าย:

```python
app.run(debug=True, host='0.0.0.0', port=8088)  # เปลี่ยนเป็น port ที่ต้องการ
```

## 🐛 การแก้ปัญหา

### 🔴 ปัญหา: Ollama ไม่เชื่อมต่อ

#### อาการ:
- เว็บไซต์แสดงข้อความ "Ollama ไม่เชื่อมต่อ"
- ไม่สามารถประเมินราคาด้วย AI ได้
- Error: "Connection refused" หรือ "Cannot connect to Ollama"

#### แนวทางแก้ไข:

**1. ตรวจสอบว่า Ollama กำลังทำงานหรือไม่**
```bash
# ลองเปิด Ollama Server
ollama serve

# ถ้าเห็นข้อความ "Listening on 127.0.0.1:11434" แสดงว่าทำงานแล้ว
```

**2. ทดสอบการเชื่อมต่อ**
```bash
# ทดสอบด้วย curl
curl http://localhost:11434/api/tags

# ถ้าได้ JSON response แสดงว่าเชื่อมต่อได้
# ถ้าได้ error แสดงว่า Ollama ไม่ทำงาน
```

**3. ตรวจสอบว่าดาวน์โหลด Model แล้วหรือยัง**
```bash
# แสดงรายการ models
ollama list

# ถ้าไม่มี model ให้ดาวน์โหลด
ollama pull llama3.2
```

**4. ตรวจสอบ Port ว่าถูกใช้งานหรือไม่**
```bash
# macOS/Linux
lsof -i :11434

# Windows
netstat -ano | findstr :11434

# ถ้ามี process อื่นใช้ port นี้ ให้ปิด process นั้น
```

**5. รีสตาร์ท Ollama**
```bash
# หยุด Ollama (กด Ctrl+C ที่หน้าต่าง ollama serve)

# หรือใช้คำสั่ง (macOS/Linux)
pkill ollama

# จากนั้นเปิดใหม่
ollama serve
```

**6. ตรวจสอบ Firewall**
```bash
# ตรวจสอบว่า Firewall ไม่บล็อก port 11434
# macOS: System Preferences > Security & Privacy > Firewall
# Windows: Windows Defender Firewall > Allow an app
# Linux: sudo ufw allow 11434
```

---

### 🔴 ปัญหา: AI ตอบช้ามาก (ใช้เวลานาน)

#### อาการ:
- รอผลการประเมินนานกว่า 2-3 นาที
- CPU ใช้งาน 100%
- หน้าจอค้างหรือแสดง Loading นาน

#### แนวทางแก้ไข:

**1. ใช้ Model ที่เล็กกว่า**
```bash
# ลบ model ใหญ่
ollama rm llama3.1

# ดาวน์โหลด model เล็ก
ollama pull llama3.2:1b  # เร็วที่สุด (1.3GB)
ollama pull llama3.2     # พอดี (2GB)
```

**2. แก้ไขไฟล์ app.py เพื่อเปลี่ยน Model**
```python
# เปิดไฟล์ app.py หาบรรทัด:
OLLAMA_MODEL = "llama3.2"

# เปลี่ยนเป็น:
OLLAMA_MODEL = "llama3.2:1b"  # model เล็กสุด
```

**3. เพิ่ม RAM หรือใช้ GPU**
- ถ้ามี GPU (NVIDIA/AMD/Apple Silicon) Ollama จะใช้อัตโนมัติ
- ปิดโปรแกรมอื่นที่ใช้ RAM มาก
- แนะนำให้มี RAM อย่างน้อย 8GB

**4. ปรับ Timeout (ถ้า AI ยังทำงานไม่เสร็จ)**
```python
# แก้ไฟล์ app.py บรรทัดประมาณ 270
response = requests.post(
    ollama_url,
    json=data,
    timeout=120  # เพิ่มจาก 60 เป็น 120 วินาที
)
```

**5. ตรวจสอบ System Resources**
```bash
# macOS
top -o cpu

# Linux
htop

# Windows
Task Manager (Ctrl+Shift+Esc)

# ดูว่า ollama ใช้ CPU/RAM เท่าไร
```

**6. ลดความซับซ้อนของคำถาม**
- ใส่ข้อมูลสั้นๆ ในช่อง "ข้อมูลเพิ่มเติม"
- ใช้คำอธิบายที่กระชับ

---

### 🔴 ปัญหา: Model ดาวน์โหลดไม่สำเร็จ

#### อาการ:
- `ollama pull` หยุดกลางคัน
- Error: "download failed" หรือ "connection timeout"

#### แนวทางแก้ไข:

**1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต**
```bash
# ทดสอบการเชื่อมต่อ
ping -c 4 ollama.ai

# ถ้า ping ไม่ผ่าน แสดงว่าเน็ตมีปัญหา
```

**2. ใช้ VPN หรือ Proxy (ถ้าถูกบล็อก)**
```bash
# ตั้งค่า proxy สำหรับ Ollama
export HTTPS_PROXY=http://your-proxy:port
ollama pull llama3.2
```

**3. ดาวน์โหลดใหม่**
```bash
# ลบ model ที่ดาวน์โหลดไม่สำเร็จ
ollama rm llama3.2

# ดาวน์โหลดใหม่
ollama pull llama3.2
```

**4. ลองใช้ Model อื่น**
```bash
# ถ้า llama3.2 ดาวน์โหลดไม่ได้ ลอง model อื่น
ollama pull llama3.2:1b  # เล็กกว่า ดาวน์โหลดเร็วกว่า
ollama pull mistral      # ทางเลือก
```

---

### 🔴 ปัญหา: Model ไม่รองรับภาษาไทย

#### อาการ:
- AI ตอบเป็นภาษาอังกฤษแม้ว่าถามเป็นไทย
- คำตอบผิดเพี้ยนหรือไม่เข้าใจคำถาม

#### แนวทางแก้ไข:

**1. ใช้ Model ที่รองรับภาษาไทย**
```bash
# Models ที่รองรับภาษาไทยดี:
ollama pull llama3.2     # รองรับไทยดีมาก
ollama pull llama3.1     # รองรับไทยดีมาก
ollama pull qwen2.5      # รองรับหลายภาษารวมไทย
ollama pull gemma2       # รองรับไทยพอใช้

# Models ที่ไม่แนะนำสำหรับไทย:
# - codellama (ออกแบบสำหรับโค้ด)
# - deepseek-coder (ออกแบบสำหรับโค้ด)
```

**2. แก้ไขไฟล์ app.py เปลี่ยน Model**
```python
# หาบรรทัดนี้ในไฟล์ app.py
OLLAMA_MODEL = "llama3.2"

# เปลี่ยนเป็น model ที่รองรับไทย
OLLAMA_MODEL = "llama3.2"  # หรือ "qwen2.5"
```

---

### 🔴 ปัญหา: Ollama ใช้ RAM มากเกินไป

#### อาการ:
- เครื่องช้ามาก
- Ollama ใช้ RAM มากกว่า 4-8 GB
- ระบบค้าง

#### แนวทางแก้ไข:

**1. ใช้ Model ขนาดเล็ก**
```bash
# ลบ model ใหญ่
ollama rm llama3.1
ollama rm gemma2:9b

# ใช้ model เล็ก
ollama pull llama3.2:1b  # ใช้ RAM น้อยที่สุด
```

**2. ปิด Ollama เมื่อไม่ใช้งาน**
```bash
# หยุด Ollama server
pkill ollama  # macOS/Linux

# หรือกด Ctrl+C ที่หน้าต่าง ollama serve
```

**3. จำกัด Memory Usage (Advanced)**
```bash
# ตั้งค่า environment variable
export OLLAMA_NUM_GPU=0  # บังคับใช้ CPU
export OLLAMA_MAX_LOADED_MODELS=1  # โหลด model ทีละตัว

ollama serve
```

---

### 🔴 ปัญหา: AI ตอบช้า

**แนวทางแก้ไข:**
- ใช้ model ที่เล็กกว่า เช่น `llama3.2:1b` แทน `llama3.1`
- เพิ่ม RAM หรือใช้ GPU
- ปรับ timeout ในไฟล์ `app.py` (บรรทัด 270)
- ปิดโปรแกรมอื่นที่ใช้ทรัพยากรเครื่องมาก

### PDF ภาษาไทยไม่แสดง

**ตรวจสอบ:**
1. ติดตั้ง xhtml2pdf และ reportlab ครบหรือไม่:
   ```bash
   pip install xhtml2pdf reportlab
   ```
2. ทดสอบสร้าง PDF:
   ```python
   from xhtml2pdf import pisa
   import io
   html = "<html><body><h1>ทดสอบภาษาไทย</h1></body></html>"
   buffer = io.BytesIO()
   pisa.CreatePDF(html.encode('utf-8'), dest=buffer, encoding='utf-8')
   ```

### Excel Upload ไม่ทำงาน

**ตรวจสอบ:**
1. ไฟล์มี columns ถูกต้อง: `province`, `property_type`, `base_price_per_sqm`
2. ชื่อจังหวัดตรงกับใน PROVINCES_DATA
3. ราคาเป็นตัวเลข (ไม่มีลูกน้ำหรือข้อความ)

### Port 8088 ถูกใช้งานแล้ว

**วิธีแก้:**
```bash
# หา process ที่ใช้ port
lsof -i :8088

# ปิด process
kill -9 <PID>

# หรือเปลี่ยน port ใน app.py
```

## 💰 ตัวอย่างราคาตามจังหวัด

### ภาคกลาง
- **กรุงเทพมหานคร**: x1.8 (แพงที่สุด)
- **สมุทรปราการ/นนทบุรี**: x1.4
- **ชลบุรี**: x1.3
- **ปทุมธานี**: x1.3

### ภาคเหนือ
- **เชียงใหม่**: x1.4
- **เชียงราย**: x1.0
- **ลำปาง**: x0.95

### ภาคอีสาน
- **นครราชสีมา**: x1.0
- **ขอนแก่น**: x0.95
- **อุดรธานี**: x0.9
- **อุบลราชธานี**: x0.9

### ภาคใต้
- **ภูเก็ต**: x1.5
- **กระบี่**: x1.1
- **พังงา**: x1.05
- **สงขลา**: x1.0

## 🔐 ความปลอดภัย

- ✅ ไม่เก็บข้อมูลส่วนบุคคล
- ✅ ข้อมูลที่อัปโหลดเก็บใน memory (หายเมื่อปิดเซิร์ฟเวอร์)
- ✅ ไม่มีการส่งข้อมูลออกนอกระบบ
- ✅ Ollama ทำงานแบบ localhost

## ⚠️ ข้อควรระวัง

**สำคัญ**:
- ระบบนี้เป็นเพียงเครื่องมือช่วยประเมินราคาเบื้องต้น
- ควรใช้ร่วมกับการประเมินจากผู้เชี่ยวชาญจริง
- ราคาที่ได้อาจแตกต่างจากราคาตลาดจริง
- ไม่ควรใช้เป็นหลักฐานทางกฎหมาย

## 📝 License

MIT License - ใช้งานได้ฟรี

## 👨‍💻 ผู้พัฒนา

**PasitDev** - ระบบประเมินราคาทรัพย์สิน

Powered by:
- [Flask](https://flask.palletsprojects.com/)
- [Ollama AI](https://ollama.ai/)
- [xhtml2pdf](https://github.com/xhtml2pdf/xhtml2pdf)
- [reportlab](https://www.reportlab.com/)
- [jQuery](https://jquery.com/)

## 🚀 การพัฒนาต่อ

### ฟีเจอร์ที่ควรเพิ่ม
- [ ] บันทึกประวัติการประเมิน (Database)
- [ ] ระบบ Login/Register
- [ ] เปรียบเทียบราคากับทรัพย์สินใกล้เคียง
- [ ] แผนที่แสดงทำเล (Google Maps API)
- [ ] กราฟแนวโน้มราคา
- [ ] API สำหรับ Mobile App
- [ ] ระบบแจ้งเตือนราคาเปลี่ยนแปลง
- [ ] รองรับหลายภาษา (อังกฤษ, จีน)

### การพัฒนา Backend
- [ ] เพิ่ม Database (PostgreSQL/MongoDB)
- [ ] Authentication & Authorization
- [ ] Rate Limiting
- [ ] Caching (Redis)
- [ ] API Versioning

### การพัฒนา Frontend
- [ ] Progressive Web App (PWA)
- [ ] Dark Mode
- [ ] Advanced Charts (Chart.js)
- [ ] Image Upload สำหรับทรัพย์สิน

---

## 📞 การติดต่อและสนับสนุน

### ต้องการความช่วยเหลือ?
- **GitHub Issues**: [สร้าง Issue](https://github.com/ezynook/Python-Asset/issues) หากพบปัญหาหรือต้องการขอคำแนะนำ
- **Discussions**: เข้าร่วมการสนทนากับชุมชน
- **Documentation**: อ่านเอกสารนี้อย่างละเอียด โดยเฉพาะส่วน [การแก้ปัญหา](#การแก้ปัญหา)

### แหล่งข้อมูลเพิ่มเติม
- **Ollama Documentation**: [https://ollama.ai/](https://ollama.ai/)
- **Ollama Models**: [https://ollama.ai/library](https://ollama.ai/library)
- **Flask Documentation**: [https://flask.palletsprojects.com/](https://flask.palletsprojects.com/)
- **xhtml2pdf Documentation**: [https://xhtml2pdf.readthedocs.io/](https://xhtml2pdf.readthedocs.io/)

### Contributing
ยินดีรับ Pull Requests! หากต้องการมีส่วนร่วมในโปรเจค:
1. Fork โปรเจค
2. สร้าง Branch ใหม่ (`git checkout -b feature/amazing-feature`)
3. Commit การเปลี่ยนแปลง (`git commit -m 'Add some amazing feature'`)
4. Push ไปยัง Branch (`git push origin feature/amazing-feature`)
5. เปิด Pull Request

---

## ⭐ Star History

ถ้าโปรเจคนี้มีประโยชน์กับคุณ อย่าลืม **Star** ⭐ ให้กับ Repository นะครับ!

---

**เวอร์ชัน**: 2.1.0
**อัปเดตล่าสุด**: 2025-11-24
**สร้างด้วย**: ❤️ และ AI
**License**: MIT
**ผู้พัฒนา**: PasitDev Team

---

## 🎓 เรียนรู้เพิ่มเติม

### Ollama Resources
- **Getting Started with Ollama**: [Ollama Quickstart](https://github.com/ollama/ollama)
- **Model Library**: [ดู Models ที่มี](https://ollama.ai/library)
- **Best Practices**: [Ollama Best Practices](https://github.com/ollama/ollama/blob/main/docs/faq.md)

### Flask Resources
- **Flask Mega-Tutorial**: [https://blog.miguelgrinberg.com/post/the-flask-mega-tutorial-part-i-hello-world](https://blog.miguelgrinberg.com/post/the-flask-mega-tutorial-part-i-hello-world)
- **Flask API Development**: [https://flask.palletsprojects.com/en/3.0.x/tutorial/](https://flask.palletsprojects.com/en/3.0.x/tutorial/)

### Python Resources
- **Python Official Docs**: [https://docs.python.org/3/](https://docs.python.org/3/)
- **Real Python**: [https://realpython.com/](https://realpython.com/)

---

หากมีคำถามหรือต้องการความช่วยเหลือ โปรดติดต่อผ่าน Issues ใน Repository นี้

**PasitDev 🚀**
